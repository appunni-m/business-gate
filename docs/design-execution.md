# Design execution record

The owner authorized execution on 10 September 2026, starting from commit `408eb9d9226072f8fda2d341e1fddb3d72ddf8e4` and public build `102001`. This record distinguishes implemented engineering work from full acceptance. No physical qualification, consenting sender, live message test or qualified adapter has been supplied. The packaged registry remains empty and connected-app actions stay disabled.

## Implemented changes

- **Exact-number focus:** a native regression reproduced keyboard focus loss after committed row refresh. Interactive rows now participate correctly in native list focus. Same-number headers and their native controls remain mounted across updates; listeners bind current account values. Bounded keyboard restoration uses data identity, receiver namespace, full phone and role, and cancels on user movement or lifecycle/context changes. Tab/Shift-Tab visit the native controls within the current row before the platform list handles row boundaries; a reproduced native vertical-navigation shortcut had skipped the summary/switch pair. Space and Enter each commit one exact-number change. A 2 dp blue/light outline makes keyboard focus visible. Accessibility focus remains controlled by Android and the screen reader.
- **Accessible row presentation:** one summary groups name, full number and state; decorative/redundant child labels are excluded. Native switches expose their current checked state. Search accepts 128 Unicode code points without splitting supplementary characters. Failed saves display the durable switch choice and distinguish the unsaved request with explicit Retry save.
- **Supporting flows:** the menu has four entries; connection and receiver management are under Compatibility & help. Unblock now requires exact-number confirmation. Positive dialog callbacks expire after relevant data/receiver/revision/authority or Activity changes. Stopped Activities dismiss dialogs, and unsent number forms restore only in the same data/receiver context. A reproduced stop-before-save lifecycle ordering bug is fixed by capturing the draft before dismissal. No confirmation or draft is automatically submitted.
- **Back behavior:** a real API 36 keyboard/IME test found that the registered Back callback was not enabled in the manifest. The Activity now explicitly enables the API 33+ callback, with the existing API 29–32 fallback. Back closes the IME, then collapses details/cancels the form, then exits while retaining choices.
- **Announcements and dismissed controls:** ordinary status refreshes are no longer a live region. A completed foreground choice emits one explicit accessibility event; a save finishing after Home stays silent. Custom controls require a shown, focused window. Settings switches retain their data/receiver scope, and an asynchronous effort result cannot reopen canceled Settings. These checks observe owned events, not spoken audio quality.
- **Actual permission lifecycle:** the debug harness enables, revokes and reconnects its own screen service through Android. It verifies epoch invalidation, retained choices, cleared one-shot grants and no restored target/session with an empty registry, then restores the prior emulator settings. Reminder cases exercise actual notification denial, grant, silent/private delivery, blocked channel, 30-day/future-clock boundaries and consent withdrawal, using only synthetic debug data.
- **Delivery:** external actions and actionlint are pinned. Verification retains an environment and per-case evidence manifest, failed/unrun cells, owned rendering hashes, minimum-API UI evidence and signed-upgrade evidence. Releases carry the hash-bound manifest. Anonymous post-publication checks verify public bytes, metadata, certificate, capabilities and versioned/rolling Git references, with finite retries. Publication failure tests cover draft resumption, each later mutation boundary, stale releases, pagination and immutable reruns.

## Measured short-screen design adjustment

At 720×1280 and 450 dpi, three-button navigation plus 200% text left a 131 px list viewport; one 48 dp target needs 135 px. Short portrait windows below 600 dp therefore move title/status into the same scrolling list. The action controls and search retain separate fixed rows in narrow portrait. Wide landscape retains its single fixed search/action row. This avoids shrinking text or squeezing search beside controls in a narrow width. The regression retains the original 48 dp viewport requirement. At larger text or short height, full dialog headings and body content share one native scroll area; headings never ellipsize. Native dialog actions stack when their measured text and padding exceed the button panel width. Android may also supply a scrollable button panel; the harness requires each entire button to be reachable, and still rejects clipping in fixed controls. Dialog width uses the activity's visible frame, excluding the side navigation bar that otherwise clipped landscape fields. Scrollable supporting content uses the explicit surface and blue focus outline so platform focus highlighting does not obscure its text contrast.

## Evidence during development

| Scenario | Observed result | Scope |
| --- | --- | --- |
| Keyboard focus regression | Focus mode passed 31 assertions after reproducing the failure. | Same-number refresh/section move, actual focus-outline pixels, Tab/Shift-Tab/Enter/Space, user movement, recycling/reset and foreground/background announcements. |
| Dialog lifecycle | Dialog mode passed 48 assertions. | Unsent Activity recreation, stale confirmations, canceled custom controls, delayed effort results, Unicode limit and seven supporting flows. |
| Actual TalkBack | Native Tab/reverse-Tab traversal passed 31 assertions with the installed service and touch exploration enabled. | Summary → exact-number switch → summary; same-number focus after refresh/section move; current switch state; no transfer to another number's switch; foreground/background accessibility-event checks. Reader-specific Alt+Right injection did not advance focus. Speech audio/announcement quality and full reading order remain unverified. Emulator accessibility settings were restored. |
| Saved-task process death | Separate probe passed 30 preparation and 14 verification assertions. | The background debug process was killed with `am kill`, its disappearance and replacement were observed, and its existing task restored query, expansion and unsent form without submitting it. This is distinct from Activity recreation and force-stop restart. |
| Actual IME/Back | Interaction mode passed 18 assertions. | Real search and number-form input connections, keyboard dismissal priority, expansion/cancellation and safe exit on API 36. |
| Screen service lifecycle | Service mode passed 17 assertions. | Actual Android enable/revoke/reconnect, durable choices, canceled authority and exact settings restoration. No qualified external route is exercised. |
| Quiet reminder | Reminder modes passed 23 and 12 assertions. | Actual own permission denial/grant, private silent notification, blocked channel, cap/time boundaries and withdrawal. The isolated debug installation is reset between cases; signed-app and receiver data are preserved. |
| Compact navigation | Both API 36 configurations passed IME/Back and 200% portrait/landscape dialog checks. | Three-button and gesture-navigation configurations, 720×1280 at 450 dpi for layouts, 52 owned images; no gesture input is injected. |
| Anonymous release verifier | Existing public build `102001` passed the new verifier. | Actual downloads, checksum, metadata, APK certificate/capability and moving tag; this does not verify unpublished changes. |
| Offline delivery/evidence tests | 20 publication, 15 public verification, 9 matrix-evidence and 8 release-metadata tests pass. | Synthetic transport/report fixtures; rejects missing/stale/dirty navigation evidence and incomplete images. No private credential is read and no synthetic record is physical evidence. |

The final local build passed the complete 42-case API 36 matrix: **950 assertions and 152 base images**, plus 126 assertions and 52 owned images across both compact navigation configurations. Actual TalkBack passed 31 assertions against the same debug APK. Pure/schema/brand, wrapper, strict lint, debug/release APKs, bundle/probe builds, artifact audits and workflow lint passed. The [local verification record](local-verification.json) preserves source-input and artifact hashes, environment, named results and performance measurements. It explicitly identifies the uncommitted source baseline; the eventual release manifest supplies the exact published commit/run binding.

The actual signed `101601` → `102001` update passed 32 preparation and 18 verification assertions on the existing API 36 emulator. After a real emulator reboot with an observed changed boot ID, the signed probe passed another 18 verification assertions. The test number, label and ALLOW choice remained saved, the visible rule stayed inactive, and the separate probe was removed. The signed application and receiver were never reset or uninstalled. This proves the stated baseline update/reboot behavior, not the unpublished new source's signed artifact.

Focused outputs live under `/tmp/business-gate/`; full reports, images and signed-update proof are under `output/`. The local record is a pre-publication checkpoint, not evidence that this source has already passed GitHub delivery. A later source edit requires the relevant checks again.

## Remaining exit conditions

The first post-implementation main run, [run 21](https://github.com/appunni-m/business-gate/actions/runs/34511883233) at `9a2071028a14e5339c1c9960c0eae8862ee25e1b`, passed the build, lint, pure rules and artifact audits but failed in the API 36 emulator step before any public per-case result appeared. Delivery was skipped. The public annotation contained only shell exit 1; anonymous log download returned HTTP 403. The diagnostic follow-up retains environment command failures and nonzero instrumentation output, adds public stage/error annotations, and tests that a partial PASS followed by transport failure cannot pass. All 14 native modes passed again locally; strict builds/lint and the pure rules passed.

[Run 22](https://github.com/appunni-m/business-gate/actions/runs/34514702899), at `ff839d173070188685025d5671837cb8af271faf`, identified an environment-collection failure: invoking the emulator's version command exited 127 because its desktop binary could not load `libpulse.so.0` on the headless runner. The collector now reads the installed emulator package's `source.properties`, as it does for the other SDK components. Actual device fingerprint/API/ABI collection and every native test gate remain required. A regression verifies metadata collection without launching the desktop emulator binary. Neither failed run published these changes; successful delivery still requires a subsequent run.

[Run 23](https://github.com/appunni-m/business-gate/actions/runs/34515588211), at `55ca18f96873ea47f3c8f8f632c5240e46b072de`, cleared collection, native tests and the base layout stage, then failed in compact navigation. Its stage annotation did not identify the exact command. A local reproduction established a harness defect: disabling the navigation overlay leaves a valid default three-button resource (`0`), but the harness rejected the lack of an enabled overlay. Android's [base navigation resource](https://android.googlesource.com/platform/frameworks/base/+/android16-release/core/res/res/values/config.xml) defines that default. The harness now captures enabled overlays and the actual resource value, supports the default selection, verifies both requested modes, and restores the exact original selection and resource. Seven offline regressions cover default/explicit selections, unavailable modes, ambiguous overlays, restoration identity and publicly retained early failures. Bounded exception details are also retained and publicly annotated. The local defect is reproduced; attribution of the CI failure remains provisional until the next run.

The default-selection regression also timed out after the second form Back. The old test used a fixed 300 ms pause without asserting that the IME had finished hiding. Owned instrumentation now observes window-inset animation completion and hidden IME state before sending the second Back; it retains exactly two Back inputs and explicit no-save/retained-choice checks. Both navigation configurations then passed from the default starting state: 130 assertions, 52 renderings, exact default-state readback and restoration of the emulator's original explicit selection. These changes affect the test harness; application source is unchanged.

The same compact matrix also passed from an explicit gesture-navigation starting selection, with 130 assertions, 52 renderings and exact selection/resource restoration. Both restoration paths are therefore measured locally; API 29 remains a CI gate for this follow-up.

The automated API 36 checkpoint is closed for the named cases. Full spoken reading/announcement quality, reader-specific shortcuts, remaining repeated-permission-prompt and live interruption/Stop cases are unverified. The source's API 29 matrix is a post-commit CI gate. A passing native Tab test does not close every manual accessibility or physical acceptance case.

The release checkpoint requires a real main-branch run using the new workflow, anonymous verification of its published APK and a local same-key signed update. Those results necessarily follow this source snapshot; use the versioned release's `build-info.json`, `verification.json`, public-verification job and workflow result for the delivered commit. A controlled rapid-push queue exercise is unrun. Run 20/build `102001` is the verified predecessor for this checkpoint.

WP6–WP9 retain their measured-environment dependencies: a consenting sender, physical receiver, compliant measured route, independent delivery observations, bounded discovery/batch evidence, final signed-artifact physical qualification and the 30-day pilot. A spare phone with a separate test number or explicitly consenting participant can supply the sender. No external account is contacted by this work.
